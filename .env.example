# ────── Lumina Backend ──────
# 后端 HTTP 端口（host 网络模式，直接用宿主机端口）
PORT=8080

# MQTT 服务器地址 (EMQX 在本机用 127.0.0.1)
MQTT_HOST=127.0.0.1

# MQTT TLS 加密 (true/false)
MQTT_TLS=false

# MQTT 端口 (明文 1883, TLS 8883)
MQTT_PORT=1883

# MQTT 认证账号密码
MQTT_USERNAME=admin
MQTT_PASSWORD=admin123
